# -*- mode: python -*-

block_cipher = None


a = Analysis(['LIMPIA_NOMBRES_INPUT.py'],
             pathex=['c:\\Users\\palonso0\\PycharmProjects\\CHEQUEO_EXTERNAS\\IMPORTA TABLAS'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='LIMPIA_NOMBRES_INPUT',
          debug=False,
          strip=False,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               name='LIMPIA_NOMBRES_INPUT')
